# TODO: Change Draggable to GSAP Draggable with Free Movement

- [x] Replace react-draggable import with GSAP Draggable import
- [x] Add useLayoutEffect import from React
- [x] Add refs for each draggable h1 element (O, R, Y, Z, A, S, A, T, I, V, A)
- [x] Add refs for parent containers to set bounds
- [x] Initialize GSAP Draggable instances in useLayoutEffect with bounds set to parent containers
- [x] Remove bounds to allow free horizontal and vertical movement
- [x] Update onDrag and onStop handlers to work with GSAP events
- [x] Ensure cursor styles and hover transitions remain functional
- [x] Test dragging behavior to confirm elements can move freely
